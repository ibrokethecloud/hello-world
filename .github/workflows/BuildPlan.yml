name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest
    env:
      SHA4: ${{github.sha::4}}

    steps:
    - uses: actions/checkout@v1
    - name: Echo calculated sha
      run: echo ${SHA4}
      #    - name: Build the Docker image
      #run: docker build . --file Dockerfile --tag gmehta3/hello-world:${SHA4}
